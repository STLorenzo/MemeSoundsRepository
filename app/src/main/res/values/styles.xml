<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Estilo simple -->
    <style name="Button" parent="@android:style/Widget.Button">
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingLeft">20dp</item>
        <item name="android:paddingRight">20dp</item>
        <item name="android:textColor">#00ff00</item>
    </style>

    <!-- fonde de boton, hereda estilo simple -->
    <style name="Button.Background">
        <item name="android:background">@drawable/button_bg</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">20sp</item>
        <item name="android:layout_margin">10dp</item>
    </style>

    <!-- Texto heredando estilo fondo y estidlo simple -->
    <style name="Button.Background.text">
        <item name="android:textColor">#FF0000</item>
    </style>

    <!-- completo -->
    <style name="Button.Background.text.dinamic">
        <item name="android:textColor">@drawable/text_blue</item>
        <item name="android:background">@drawable/button_bg</item>
        <item name="android:shadowColor">#ef0b0b</item>
        <item name="android:shadowDx">3</item>
        <item name="android:shadowDy">3</item>
        <item name="android:shadowRadius">2</item>
    </style>

</resources>
